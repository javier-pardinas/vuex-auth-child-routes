import Vue from 'vue'
import Vuex from 'vuex'

//MODULES
import auth from '@/modules/authentication';

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
